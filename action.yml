name: Fyx Autodoc
description: An automated method to generate in-code api skeletons
inputs:
  dir:
    description: "Working directory"
    required: true
  type:
    description: "Operation selection"
    required: true
branding:
  icon: 'archive'
  color: 'blue'
runs:
  using: 'docker'
  image: 'Dockerfile'
